# API REST pública (stub)

Extensión futura: API de solo lectura (ventas, reportes, stock) autenticada con API key.

- **No** hay controllers expuestos en esta fase.
- Activación prevista: `PUBLIC_API_ENABLED=true` (aún no cableado).
- Ver interfaz stub en `public-api.stub.ts`.
